import { tree_0, tree_1 } from "asset/Icons";
import { StyledRoot } from "./style";
const Carbon = () => {
  return (
    <StyledRoot>
      <img src={tree_1} />
    </StyledRoot>
  );
};

export default Carbon;
