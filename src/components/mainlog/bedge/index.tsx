import { BedgeIcon } from "asset/Icons";
import { StyledRoot } from "./style";
const Bedge = () => {
  return (
    <StyledRoot>
      <img src={BedgeIcon} />
    </StyledRoot>
  );
};

export default Bedge;
